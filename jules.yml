sealId: 102565
node: java180u161-maven352
blackduck:
  localScanCredentials: CIBCL_BITBUCKET_CREDENTIAL
  criteria: BuildStatusForSealViolation=NONE,VulnerabilityThresholdForUnstable=MEDIUM,VulnerabilityThresholdForError=HIGH
  credentials: CIBCL_BLACK_DUCK_CREDENTIAL
  artifactFilePatterns: '*target/my-dependencies/*.jar'
buildConfig:
  toolSettings: cobertura:cobertura dependency:copy-dependencies --batch-mode -DincludeScope=runtime -DexcludeGroupIds=org.jenkins-ci.plugins,org.jenkins-ci -DoutputDirectory=target/my-dependencies -Dformat=xml
ssap:
  credentials: CIBCL_SSAP_CREDENTIAL
frs:
  credentials: CIBCL_FRS_CREDENTIAL
